// Zed keymap
//
// For information on binding keys, see the Zed
// documentation: https://zed.dev/docs/key-bindings
//
// To see the default key bindings run `zed: open default keymap`
// from the command palette.
[
  {
    "context": "VimControl && !menu",
    "bindings": {
      // Put key bindings here if you want them to work in normal & visual mode.
      "shift-k": "editor::Hover",
      "g r a": "editor::ToggleCodeActions",
      "g r d": "editor::GoToDefinition",
      "g r r": "editor::GoToNextReference",
      "space l f": "editor::Format",
      "space l r": "editor::Rename",
      "space o": "tab_switcher::Toggle",
      "\\": "workspace::ToggleRightDock",
      "ctrl-s": "workspace::Save",
      "s a": ["vim::PushAddSurrounds", {}],
      "s c": ["vim::PushChangeSurrounds", {}],
      "s d": ["vim::PushDeleteSurrounds", {}],
    },
  },
  {
    "context": "vim_mode == normal && !menu",
    "bindings": {
      "shift-y": ["workspace::SendKeystrokes", "y $"], // Use neovim's yank behavior: yank to end of line.
    },
  },
  {
    "context": "vim_mode == insert",
    "bindings": {
      "j k": "vim::NormalBefore", // In insert mode, make jk escape to normal mode.
      "ctrl-s": ["workspace::SendKeystrokes", "escape :w"],
      "cmd-s": ["workspace::SendKeystrokes", "escape :w"],
    },
  },
  {
    "context": "EmptyPane || SharedScreen",
    "bindings": {
      // Put key bindings here (in addition to the context above) if you want them to
      // work when no editor exists.
      // "space f": "file_finder::Toggle"
    },
  },
]
